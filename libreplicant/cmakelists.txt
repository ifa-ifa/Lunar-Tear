set(LIBREPLICANT_SOURCES
    src/stbl/stbl.cpp
    src/bxon/bxon.cpp
    "src/bxon/archive_param.cpp"
 "src/arc/arc.cpp" "src/pack/pack.cpp" "include/replicant/bxon/error.h" "include/replicant/bxon/archive_param_builder.h" "src/bxon/archive_param_builder.cpp" "src/bxon/common.h")

  find_package(zstd CONFIG REQUIRED)

add_library(libreplicant STATIC ${LIBREPLICANT_SOURCES})

target_include_directories(libreplicant PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/include)


target_include_directories(libreplicant
    PUBLIC
        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
        $<INSTALL_INTERFACE:include>
)

target_link_libraries(libreplicant zstd::libzstd) 